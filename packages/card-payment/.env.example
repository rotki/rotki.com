# Web API URL for backend calls (used only for proxy configuration)
# In development, this should point to your local Django backend
# In production, this should point to the production API
WEBAPI_URL=http://127.0.0.1:8000